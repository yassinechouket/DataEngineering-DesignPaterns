# Extra deps the `rag` package needs on top of the base Airflow image.
# Baked into the image via Dockerfile — pinned so rebuilds are reproducible.
langchain-text-splitters==0.3.8   # chunking.RecursiveCharacterTextSplitter
pymupdf==1.26.7                   # extraction: `import fitz`
fastembed==0.8.0                  # embeddings, EMBEDDING_PROVIDER=local (ONNX, no API key)
voyageai==0.3.6                   # embeddings, EMBEDDING_PROVIDER=voyage